# Default values for springboot-example.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
frontend:
  image: dockeridarif3/angular-frontend:latest
  
image:
  repository: dockeridarif3/springboot-backend
  tag: latest
  pullPolicy: IfNotPresent

extraEnv:
  - name: DATABASE
    value: employee_management_system


fullname: "arif-backend"
nameOverride: "custom-backend"
fullnameOverride: ""

service:
#  type: SERVICE_TYPE
  type: NodePort
  port: 8080

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts:
    - chart-example.local
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
